<view class="container join-main">
	<view class="justjoin" catchtap="_dispatch" data-catchtap="joinBind" data-path="">
		<text>立即加入</text>
	</view>
	<view class="input-area" wx:for="{{persons}}" wx:for-index="idx">
		<view class="sec-con">
			<view class="section">
				<view class="section_title">姓名</view>
				<input bindinput="_dispatch" placeholder="例如：张三" data-idx="{{idx}}" data-name="name" value="{{item.name?item.name:''}}" data-bindinput="bindKeyInput" data-path=""/>
			</view>
			<view class="section">
				<view class="section_title">证件号码</view>
				<input bindinput="_dispatch" placeholder="身份证号" data-idx="{{idx}}" data-name="cardCode" maxlength="18" value="{{item.cardCode?item.cardCode:''}}" data-bindinput="bindKeyInput" data-path=""/>
			</view>
		</view>
		<view class="del-btn" catchtap="_dispatch" data-idx="{{idx}}" data-catchtap="removePerson" data-path="">
			<icon class="del-icon"/>
		</view>
	</view>
	<view class="add-btn" catchtap="_dispatch" data-catchtap="addPerson" data-path="">
		<icon class="icon-add iconfont icon-roundadd"/>
		<text> 继续添加</text>
	</view>
	<!-- <view class="money">
		<view class="t">
			<text>选择金额</text>
		</view>
		<view class="num">
			<text>9元</text>
		</view>
	</view> -->
	<view class="intro">
		<view><text>{{conditionTxt}}</text></view>
		<view class="tip">
			请在加入本互助社群前咨询家人，避免重复加入。
		</view>
	</view>
	<view class="checkbox-group">
    <checkbox-group bindchange="_dispatch" data-bindchange="checkChange" data-path="">
		<view>
			<label class="checkbox">
			    <checkbox disabled="true" checked="{{publicChecked}}"/>已同意 <text class="text">{{publicConvention}}</text>
			</label>
		</view>
		<view>
			<label class="checkbox">
		    	<checkbox disabled="true" checked="{{checked}}"/>已同意 <text class="text">{{conventionTxt}}</text>
		  	</label>
		</view>
    </checkbox-group>
	</view>
</view>