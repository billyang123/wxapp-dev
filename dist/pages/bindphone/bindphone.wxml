
<view class="container">
	<view class="warn-tips">
		<icon class="icon" type="warn" size="28" color="orange"/>
		<text class="text">为了你的权益得到保障，建议你绑定手机号码</text>
	</view>
	<view class="bind-top">
		<view class="head">
			<image class="img" src="{{userInfo.avatarUrl}}"/>
		</view>
		<view class="name">
			<text>{{userInfo.nickName}}</text>
		</view>
		<!-- <view class="note">
			<icon class="icon-tip"></icon>
			<text>为了使用顺畅，请绑定您的手机号</text>
		</view> -->
	</view>
	<view class="bind-form">
		<form>
			<view class="input-area">
				<view class="section">
					<view class="section_title">手机号</view>
					<input bindinput="_dispatch" placeholder-style="color:#ddd" style="width:540rpx;" placeholder="输入手机号码" data-name="phoneValue" maxlength="11" data-bindinput="bindKeyInput"/>
				</view>
				<view class="section pcode">
					<view class="section_title">验证码</view>
					<input bindinput="_dispatch" placeholder-style="color:#ddd" placeholder="输入验证码" data-name="codeValue" maxlength="6" data-bindinput="bindKeyInput"/>
					<view class="{{cDisCls}}" bindtap="_dispatch" data-bindtap="getCheckCode">{{btnText}}</view>
				</view>
			</view>
			<view class="bind-btn">
				<button class="submitbtn {{disabled?'disabled':''}}" disabled="{{disabled}}" bindtap="_dispatch" data-bindtap="doBind">绑定</button>
			</view>
		</form>
	</view>
</view>